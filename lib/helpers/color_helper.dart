import 'package:flutter/material.dart';

class ColorHelper {
  static const Color primary = Color(0xFF26187C);
  static const Color accent = Color(0xFF4CAF50);
  static const Color background = Color(0xFFFFFFFF);
  static const Color secondaryLight = Color(
    0xFFAFB8FF,
  ); // Light purple for active states

  // Splash screen colors
  static const Color lightBlue = Color(0xFF7589FD); // Lightning bolt color

  // Text colors
  static const Color textPrimary = Color(0xFF030311); // Black text
  static const Color textSecondary = Color(
    0xFF5D5A6E,
  ); // Gray placeholder/secondary text

  // Border colors
  static const Color borderColor = Color(
    0xFF5D5A6E,
  ); // Border color with opacity

  // Map and filter colors
  static const Color mapBackground = Color(0xFFF1F2F6); // Light gray background
  static const Color filterChipInactive = Color(
    0xFFF1F2F6,
  ); // Inactive filter chip background

  // Error and logout colors
  static const Color logoutColor = Color(0xFFE6124E); // Red color for logout
}
